'use strict';

/*
  there are some lines in this program that are out of order
  you need to put them back in order to pass the tests
*/

'use strict';

/*

  fill in the blanks!

*/

const text = 'asdf';
let backwardsText = '';

// -- begin mixed-up lines --

  text += nextLetter;
}
for (let i = text.length - 1; i >= 0; i--) {
  const nextLetter = text[i];

// -- end mixed-up lines --

console.assert(backwardsText === 'asdf');
